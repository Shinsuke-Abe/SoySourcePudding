buildscript {
    ext {
        deeplearning4jVersion = '0.5.0'
        nd4jVersion = '0.5.0'
        kuromojiVersion = '0.9.0'
    }

    repositories {
        mavenCentral()
    }
}

subprojects {
    group 'shinsuke-abe'
    version '1.0-SNAPSHOT'

    apply plugin: 'java'

    sourceCompatibility = 1.8

    repositories {
        mavenLocal()
        mavenCentral()
    }

    dependencies {
        compile group: 'com.atilika.kuromoji', name: 'kuromoji-ipadic', version: '0.9.0'
        compile group: 'org.deeplearning4j', name: 'deeplearning4j-ui', version: '0.5.0'
        compile group: 'org.deeplearning4j', name: 'deeplearning4j-nlp', version: '0.5.0'
        compile group: 'org.nd4j', name: 'nd4j-native-platform', version: '0.5.0'
        testCompile group: 'junit', name: 'junit', version: '4.11'
    }

    // 各種ディレクトリ生成
    task mkdirs << {
        ["src/main/java", "src/main/resources", "src/test/java", "src/test/resources"].each {
            def path = "${projectDir}/${it}"
            ant.mkdir(dir: path)
            ant.touch(file: "${path}/.gitkeep")
        }
    }
}

project(':learning.backend') {
    dependencies {

    }
}